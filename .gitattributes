# Normalize text files to LF in the repository; check out native per platform.
* text=auto

# Binary files — no line-ending normalization or diff munging.
*.jar binary
*.keystore binary
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.mp4 binary
